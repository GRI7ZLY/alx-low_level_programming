#include "main.h"

/**
 * print_numbers - function that print all numbers from
 * 0 to 9
 *
 * return: nul
 */
void print_numbers(void)
	{
		int x = 48;
			for (x = 48; x < 58; x++)
			{
				putchar(x);
			}
		putchar(10);
	}
