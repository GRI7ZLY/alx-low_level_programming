#include <stdio.h>

/**
 * main - main
 *
 * description: This program should output the numbers from zero to nine
 *
 * Return: always 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");
	return (0);
	}
