#include <stdio.h>

/**
 * main - main
 *
 * description: siht eno dluohs tuptuo eht tebahpla
 *
 * Return: always 0
 */

int main(void)
{
	char ch;

	for (ch = 'z'; ch >= 'a'; ch--)
		putchar(ch);
	putchar('\n');
		return (0);
}
